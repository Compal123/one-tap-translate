PySide6>=6.8
mss>=9.0
numpy
opencv-python
deep-translator>=1.11
requests

# ── OCR: 2 backend, app tự chọn theo máy (đổi được trong Cài đặt > Quét & OCR) ──
# 1) RapidOCR (ONNX Runtime, model PP-OCRv6 small) - cho máy chỉ có CPU.
rapidocr>=2.0
onnxruntime
# 2) PP-OCRv5 (paddleocr) - chính xác nhất, khuyên dùng khi có GPU NVIDIA.
#    paddlepaddle-gpu KHÔNG cài từ PyPI - phải lấy từ index của Paddle theo CUDA:
#      pip install paddlepaddle-gpu -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#    Máy chỉ CPU có thể `pip install paddlepaddle` nhưng chậm - nên dùng RapidOCR.
paddleocr>=3.0
